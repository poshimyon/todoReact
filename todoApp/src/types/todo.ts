export type TodoType = {
    id: string;
    title: string;
    author: string;
    todoDate: string;
};

export type TodoUpdatePayload = {
    title: string;
    author: string;
    todoDate: string;
};
